# พระมหาคัมภีร์อัลกุรอานแปลคำต่อคำ (WBW Thai Master Audit Methodology)
## มาตรฐานและคู่มือการตรวจสอบชำระล้างคำแปลคำต่อคำฉบับสมบูรณ์ (83,665 คำ)

---

## 1. บทนำและปรัชญาพื้นฐาน (Philosophy & Core Principles)

การแปลพระมหาคัมภีร์อัลกุรอานแบบคำต่อคำ (Word-by-Word: WBW) มีความละเอียดอ่อนทางภาษาและหลักการศาสนาอย่างสูงสุด หัวใจสำคัญที่สุดคือ:

> **"1 ความผิดพลาด คือมากเกินไป (Zero-Tolerance: 1 mistake is too many)"**

### กฎทองคำ 3 ประการ (The 3 Golden Rules):
1. **หลักการ 1-to-1 Word Alignment:** แต่ละคำในภาษาอาหรับต้องมีคำแปลภาษาไทยที่ตรงกับตำแหน่งคำนั้นๆ อย่างแท้จริง ห้ามนำความหมายของคำข้างเคียงมาควบรวม (Neighbor Absorption) หรือแปลข้ามคำ (Word Inversion) จนทำให้คำใดคำหนึ่งสูญเสียความหมายหรือกุดด้วน
2. **ขจัดปรากฏการณ์โดมิโน (Domino Shift / Gap-Filling Elimination):** ในภาษาอาหรับมีคำอนุภาค (Particles) หลายคำที่ต้องการคำข้างหน้าหรือข้างหลังมาเติมเต็มความหมาย (เช่น `هَلْ` แปลว่า "จะ...ไหม" หรือ `أَلَّا` แปลว่า "ว่าจะไม่") นักแปลในอดีตมักแปลคำถัดไปควบความหมาย แล้วทิ้งให้คำอนุภาคกลายเป็นคำเชื่อมโดดเดี่ยว เช่น เหลือแค่คำว่า "ว่า", "และ", หรือ "ที่" ซึ่งทำให้เกิดการเลื่อนหลุด (Shift) ตลอดทั้งอายะฮ์
3. **การประสานข้อมูล 4 ทางพร้อมกัน (4-Way Data Synchronization):** เมื่อตรวจพบและแก้ไขคำใด จะต้องอัปเดตลง:
   - ฐานข้อมูล SQLite หลัก (`quran_offline.db`)
   - ไฟล์ Master JSON ทั้งสองชุด (`quran_wbw_th_en_fixed.json` และ `quran_wbw_th_en_MASTER.json`)
   - ไฟล์ Web JSON ทั้ง 114 ซูเราะฮ์ (`thai-quran-web/public/data/wbw/*.json`)
   - หน้าจอตรวจสอบเปรียบเทียบคำต่อคำ 83,665 คำ (`wbw_comparison.html`)
   - และต้อง Commit + Push ขึ้น GitHub สู่ Production ทันทีทุกรอบ

---

## 2. เสาหลักของเครื่องมือตรวจสอบ (The 4 Audit Engines)

### เครื่องยนต์ที่ 1: Semantic Cascade & Gap-Filling Auditor
* **เป้าหมาย:** สแกนหาคำกริยา (Verb) หรือคำนาม (Noun) ที่ถูกตัดทอน (Truncated) เหลือเพียงคำเชื่อมเดี่ยว เช่น `"และ"`, `"ว่า"`, `"ที่"`, `"ของ"`, `"ใน"`, `"ณ"`
* **สาเหตุ:** เกิดจากการที่คำข้างเคียงกลืนความหมายของกริยาไป เช่น `وَيَقُولُ` (และพวกเขากล่าวว่า) ถูกลดทอนเหลือแค่ `"และ"` เพราะคำถัดไปถูกแปลว่า `"ศรัทธากล่าวว่า"`
* **การแก้ไข:** แยกคืนคำกริยากลับมาประจำตำแหน่งเดิม 1-to-1

### เครื่องยนต์ที่ 2: Mutashabihat Cross-Ayah Consistency Engine
* **เป้าหมาย:** ตรวจสอบความสอดคล้องของวลีและสำนวนที่ซ้ำกัน (Mutashabihat - 1,550 อายะฮ์ทั่วทั้งคัมภีร์)
* **ตัวอย่าง:** สำนวน `وَمَآ أَدْرَىٰكَ مَا...` (และสิ่งใดเล่าจะทำให้เจ้ารู้ได้ว่า...คืออะไร) ใน 13 ซูเราะฮ์:
  - `w1: وَمَآ` = `"และสิ่งใดเล่า"`
  - `w2: أَدْرَىٰكَ` = `"ที่จะทำให้เจ้ารู้ได้"`
  - `w3: مَا` = `"ว่าอะไรคือ"`
  - `w4+: ...` = คำนามหลักประจำอายะฮ์นั้นๆ ห้ามนำคำว่า "คืออะไร" มากลืนไว้ในคำนาม

### เครื่องยนต์ที่ 3: Cross-Lingual Part-of-Speech Triangulation
* **เป้าหมาย:** ตรวจสอบประเภทคำ (POS) เปรียบเทียบ 3 ภาษา (อาหรับ - อังกฤษ - มลายู - ไทย)
* **ตัวอย่าง:**
  - หากอาหรับเป็น `Interrogative Particle` (คำถาม) คำแปลไทยต้องมีคำแสดงคำถาม (`...ไหม`, `หรือ`, `สิ่งใด`, `ไฉน`, `เมื่อใด`)
  - หากอาหรับเป็น `Negative Particle` (ปฏิเสธ) คำแปลไทยต้องมีคำปฏิเสธ (`ไม่`, `มิได้`, `หามิได้`, `อย่า`)
  - ตรวจสอบบุรุษสรรพนาม (Pronoun Concordance): หากอาหรับเป็นสรรพนามบุรุษที่ 2 พหูพจน์ (`-kum`, `antum`) ภาษาไทยต้องใช้ `"พวกเจ้า / พวกท่าน"` ห้ามหลุดเป็นสรรพนามบุรุษที่ 3 `"พวกเขา"`

### เครื่องยนต์ที่ 4: Definitive Corpus Sanitization (การชำระล้างขยะข้อมูล)
* **Foreign Glyph Detection:** สแกนตรวจจับอักขระแปลกปลอมจากการสแกน OCR โบราณ เช่น ตัวอักษรฮีบรู (Hebrew: `ב`, `ג`, `כ`), อักษรเกาหลี (Hangul: `조`), หรืออักษรจีน (CJK)
* **Slash Gloss Elimination:** ชำระล้างเครื่องหมายทับ `/` ที่มาจากการนำศัพท์สองความหมายมาต่อกัน (เช่น `"คุยโม้โอ้อวด/จ้วงจาบ"` $\rightarrow$ ปรับเป็นภาษาไทยที่สละสลวยและถูกต้องตามตัฟซีร `"พูดจ้วงจาบไร้สาระ"`)
* **Single-Character Truncation:** ตรวจจับคำเดี่ยวตัวอักษรเดียว เช่น คำว่า `عِندَ` ที่ถูกตัดทอนเหลือแค่ `"ณ"` $\rightarrow$ ปรับเป็น `"ณ ที่"`
* **Dictionary Meta-Leak Removal:** ล้างคำแปลศัพท์ดิบจากพจนานุกรม เช่น คำว่า `كَلِمَة` แปลหลุดเป็น `"พจนานุกรม"` $\rightarrow$ ปรับเป็น `"พระดำรัส / โองการ"` หรือคำแปลสมัยใหม่ที่ไม่เหมาะสม เช่น `"ฯพณฯ"`, `"คนส่งพัสดุ"`, `"รอลูกศิษย์"`

---

## 3. ขั้นตอนการรัน Batch Audit (Step-by-Step Execution)

### กฎการเรียงลำดับคำ (Canonical Sorting Law - CRITICAL):
ในฐานข้อมูล SQLite ข้อมูลในตาราง `words` **ไม่ได้เรียงตามลำดับหน้าหรือลำดับอายะฮ์ในมุศฮัฟ (Mushaf Order) โดย ID** (เนื่องจากต้นฉบับนำเข้าอายะฮ์ที่ 1 ของทุกซูเราะฮ์ก่อน แล้วตามด้วยอายะฮ์ที่ 2)  
**ดังนั้น ทุกสคริปต์ต้องจัดเรียงข้อมูลในหน่วยความจำด้วย Sort Key เสมอ:**
```python
def sort_key(w):
    s, a = map(int, w['verse_key'].split(':'))
    return (s, a, w['position'])

all_words_sorted = sorted(all_words, key=sort_key)
```

### การแบ่งขนาด Batch (Batch Sizing):
* **Macro Audit (รอบภาพรวม):** `batch = 500 คำ` (168 Batches สำหรับทั้งคัมภีร์ 83,665 คำ)
* **Fine-Grained Audit (รอบตรวจละเอียดระดับไมโคร - แนะนำสำหรับรอบถัดไป):** `batch = 100 คำ` (837 Batches)
  * เหมาะสำหรับการตรวจสอบคำอย่างละเอียด โดยเฉพาะในยุซอัมมา หรือโองการที่มีบทสนทนาหนาแน่น

### กฎการทำซ้ำ (The Repeat / Double-Check Rule):
หากในการรัน Batch ใด พบว่ามี Anomaly หนาแน่นผิดปกติ หรือมีคำแปลกปลอมหลุดเข้ามา **ห้ามก้าวข้ามไป Batch ถัดไปทันทีเด็ดขาด** ให้ดำเนินการ:
1. วิเคราะห์สาเหตุของความผิดพลาดใน Batch นั้น
2. ออกแบบคำแปลที่ถูกต้องตรงตามไวยากรณ์อาหรับและบริบทศาสนา
3. ทำการแก้ไขและอัปเดตระบบ
4. **รัน Deep Re-Scan ซ้ำใน Batch นั้นทันที** จนกระทั่งผลลัพธ์ยืนยันว่า **`Remaining Anomalies = 0`** จึงจะอนุญาตให้ขยับไป Batch ถัดไป

---

## 4. แผนการบำรุงรักษาประจำ (Regular Maintenance / CI Checklist)

เมื่อมีการปรับปรุงคำแปลหรือต้องการ Audit ซ้ำตามรอบระยะเวลา ให้ปฏิบัติตาม Checklist ดังนี้:

- [ ] **Step 1:** ตรวจสอบสถานะใน `mushaf_audit_state.json`
- [ ] **Step 2:** รันสคริปต์สแกนเนอร์ตามขนาด Batch ที่กำหนด (เช่น `python run_wbw_audit.py --batch-size 100`)
- [ ] **Step 3:** ตรวจสอบรายการที่ระบบตั้ง Flag เตือน (Anomalies)
- [ ] **Step 4:** ตรวจสอบบริบทโองการกับตัฟซีรมาตรฐาน (Ibn Kathir, Al-Jalalayn, สมาคมนักเรียนเก่าอาหรับ)
- [ ] **Step 5:** บันทึกคำแปลที่แก้ไขลง SQLite (`quran_offline.db`)
- [ ] **Step 6:** Sync ลง Master JSONs (`fixed` & `MASTER`)
- [ ] **Step 7:** รันสคริปต์ Export สำหรับ Web App (`export_db_for_web.py`)
- [ ] **Step 8:** คอมไพล์ HTML Standalone Viewer (`build_complete_all_words_viewer.py`)
- [ ] **Step 9:** รัน `full_quran_grand_audit.py` เพื่อยืนยันว่าทั้ง 83,665 คำไม่มีข้อผิดพลาดหลงเหลือ
- [ ] **Step 10:** Git Commit และ Push ขึ้นทั้ง 3 Repository:
  - `H:/gits/thai-quran-app`
  - `H:/gits/to fix the translation (headers from QUL)`
  - `H:/gits/thai-quran-web`

---

*เอกสารฉบับนี้จัดทำขึ้นเพื่อเป็นคู่มือมาตรฐานสากลในการตรวจสอบคำแปลคำต่อคำภาษาไทย สำหรับทีมนักวิชาการและระบบ Automated Audit Engine ต่อไปในอนาคต*
